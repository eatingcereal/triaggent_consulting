/** @type {import('next').NextConfig} */
const nextConfig = {
  // To serve the atlas under a path (e.g. https://triaggent.com/sgo), uncomment
  // the line below AND prefix the fetch URL in public/app.js with the same path.
  // basePath: '/sgo',
};

export default nextConfig;
